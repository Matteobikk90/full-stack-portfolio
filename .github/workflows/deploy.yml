name: Deploy to EC2 via Registry

on:
  push:
    branches: [main, dev]

concurrency:
  group: deploy-ec2
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    env:
      IMAGE_TAG: ${{ github.sha }}
      REGISTRY: ghcr.io/matteobikk90
      FOLDER: full-stack-portfolio

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🔐 Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐳 Build client image
        run: |
          docker build -t $REGISTRY/$FOLDER-client:$IMAGE_TAG -f apps/client/Dockerfile apps/client
          docker tag $REGISTRY/$FOLDER-client:$IMAGE_TAG $REGISTRY/$FOLDER-client:latest

      - name: 🐳 Build server image
        run: |
          SERVER_SUFFIX=$( [ "${{ github.ref_name }}" = "main" ] && echo "prod" || echo "dev" )
          docker build -t $REGISTRY/$FOLDER-server-$SERVER_SUFFIX:$IMAGE_TAG -f apps/server/Dockerfile apps/server
          docker tag $REGISTRY/$FOLDER-server-$SERVER_SUFFIX:$IMAGE_TAG $REGISTRY/$FOLDER-server-$SERVER_SUFFIX:latest

      - name: 📤 Push images to registry
        run: |
          docker push $REGISTRY/$FOLDER-client:$IMAGE_TAG
          docker push $REGISTRY/$FOLDER-client:latest

          SERVER_SUFFIX=$( [ "${{ github.ref_name }}" = "main" ] && echo "prod" || echo "dev" )
          docker push $REGISTRY/$FOLDER-server-$SERVER_SUFFIX:$IMAGE_TAG
          docker push $REGISTRY/$FOLDER-server-$SERVER_SUFFIX:latest

      - name: 🚀 Deploy on EC2 via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          script: |
            set -e
            REGISTRY=ghcr.io/matteobikk90
            FOLDER=full-stack-portfolio
            TAG=${{ github.sha }}

            if [ "${{ github.ref_name }}" = "main" ]; then
              SERVICE_NAME="server-prod"
            else
              SERVICE_NAME="server-dev"
            fi

            echo "📥 Pulling images from GHCR..."
            docker pull $REGISTRY/$FOLDER-client:$TAG
            docker pull $REGISTRY/$FOLDER-$SERVICE_NAME:$TAG

            echo "🚀 Starting containers..."
            export GITHUB_SHA=$TAG
            docker compose -f ~/full-stack-portfolio/docker-compose.yml up -d $SERVICE_NAME client

            echo "🧹 Cleaning unused Docker resources..."
            docker system prune -af --volumes
